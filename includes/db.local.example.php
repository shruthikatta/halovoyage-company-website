<?php
/**
 * Loaded only on local development (CLI, `php -S`, or SERVER_NAME localhost/127.0.0.1).
 * Uploaded copies are ignored on infinityfree/epizy so production uses credentials in db.php instead.

$host = '127.0.0.1';
$user = 'root';
$password = '';
$database = 'halovoyage';

// Uncomment if MySQL listens on a custom port (MAMP MySQL often uses 8889):
// $db_port = 8889;
